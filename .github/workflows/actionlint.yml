---
name: Actions Linter

# This workflow is now integrated into required-status-checks.yml
# Keeping it for manual triggers only
on:
  workflow_dispatch:

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: actionlint
        uses: raven-actions/actionlint@v1
